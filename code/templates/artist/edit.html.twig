{% extends 'base.html.twig' %}

{% block title %}Éditer artiste{% endblock %}

{% block body %}
    <div class="container">
        <div class="part">
            <h1>Éditer un artiste</h1>

            {{ include('artist/_form.html.twig', {'button_label': 'Update'}) }}

            <a class="btn btn-dark" href="{{ path('app_artist_index') }}">Retour</a>

            {{ include('artist/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}
